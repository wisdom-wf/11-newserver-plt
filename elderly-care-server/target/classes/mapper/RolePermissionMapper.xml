<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elderlycare.mapper.RolePermissionMapper">

    <resultMap id="BaseResultMap" type="com.elderlycare.entity.RolePermission">
        <id column="role_permission_id" property="rolePermissionId"/>
        <result column="role_id" property="roleId"/>
        <result column="permission_id" property="permissionId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="batchInsert">
        INSERT INTO t_role_permission (role_permission_id, role_id, permission_id, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.rolePermissionId}, #{item.roleId}, #{item.permissionId}, NOW())
        </foreach>
    </insert>

    <delete id="deleteByRoleId">
        DELETE FROM t_role_permission WHERE role_id = #{roleId}
    </delete>

    <select id="selectPermissionIdsByRoleId" resultType="java.lang.String">
        SELECT permission_id FROM t_role_permission WHERE role_id = #{roleId}
    </select>

</mapper>
