<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elderlycare.mapper.statistics.StatisticsMapper">

    <select id="selectOrderTrendByDateRange" resultType="map">
        SELECT
            <choose>
                <when test="groupBy == 'day'">
                    DATE(create_time) AS date
                </when>
                <when test="groupBy == 'week'">
                    DATE_FORMAT(create_time, '%Y-W%u') AS date
                </when>
                <when test="groupBy == 'month'">
                    DATE_FORMAT(create_time, '%Y-%m') AS date
                </when>
                <otherwise>
                    DATE(create_time) AS date
                </otherwise>
            </choose>,
            COUNT(*) AS orderCount,
            SUM(CASE WHEN status = 'COMPLETED' THEN 1 ELSE 0 END) AS completedCount
        FROM t_order
        WHERE deleted = 0
        <if test="startDate != null">
            AND create_time >= #{startDate}
        </if>
        <if test="endDate != null">
            AND create_time &lt;= #{endDate}
        </if>
        GROUP BY <choose>
            <when test="groupBy == 'day'">DATE(create_time)</when>
            <when test="groupBy == 'week'">DATE_FORMAT(create_time, '%Y-W%u')</when>
            <when test="groupBy == 'month'">DATE_FORMAT(create_time, '%Y-%m')</when>
            <otherwise>DATE(create_time)</otherwise>
        </choose>
        ORDER BY date
    </select>

    <select id="selectOrderStatisticsByDateRange" resultType="map">
        SELECT
            service_type_code AS serviceTypeCode,
            service_type_name AS serviceTypeName,
            COUNT(*) AS orderCount,
            SUM(CASE WHEN status = 'COMPLETED' THEN 1 ELSE 0 END) AS completedCount,
            COALESCE(SUM(estimated_price), 0) AS totalAmount,
            COALESCE(AVG(rating), 0) AS averageRating
        FROM t_order
        WHERE deleted = 0
        <if test="startDate != null">
            AND create_time >= #{startDate}
        </if>
        <if test="endDate != null">
            AND create_time &lt;= #{endDate}
        </if>
        <if test="serviceTypeCode != null and serviceTypeCode != ''">
            AND service_type_code = #{serviceTypeCode}
        </if>
        GROUP BY service_type_code, service_type_name
        ORDER BY orderCount DESC
    </select>

</mapper>
