# Mac 清理扫描报告

**扫描时间**: 2026-04-27 02:40  
**磁盘使用情况**: 230GB 总量，12GB 已用，14GB 可用（使用率 46%）  
**可清理空间估算**: ~10GB

---

## ⚠️ 安全提示

本报告**仅列出可清理项目**，不会自动执行任何删除操作。  
**所有清理操作需要你确认后才能执行**。

---

## 📊 扫描结果总览

| 目录 | 当前大小 | 可清理估算 | 风险等级 |
|------|----------|------------|----------|
| ~/Library/Caches | 17GB | ~10GB | 低 |
| ~/Library/Logs | 37MB | ~30MB | 低 |
| ~/Downloads | 185MB | ~173MB | 中（需确认文件）|
| Homebrew 缓存 | 1.5GB | ~1.5GB | 低 |
| pip 缓存 | 1.3GB | ~1.3GB | 低 |
| Playwright 缓存 | 1.9GB | ~1.9GB | 低 |
| **总计** | **~21GB** | **~13GB** | - |

---

## 🗂️ 详细清理建议

### 1. 应用缓存（~/Library/Caches）⭐ 优先清理

| 应用 | 缓存大小 | 清理命令 | 说明 |
|------|----------|----------|------|
| **ms-playwright** | 1.9GB | `rm -rf ~/Library/Caches/ms-playwright/*` | Playwright 浏览器缓存，可安全清理 |
| **LarkShell** | 1.9GB | `rm -rf ~/Library/Caches/LarkShell/*` | 飞书 Shell 缓存 |
| **com.yingdao.yd.ShipIt** | 1.5GB | `rm -rf ~/Library/Caches/com.yingdao.yd.ShipIt/*` | 影刀 RPA 缓存 |
| **Homebrew** | 1.5GB | `brew cleanup -s` | Homebrew 下载缓存 |
| **pip** | 1.3GB | `rm -rf ~/Library/Caches/pip/*` | Python pip 缓存 |
| **com.openai.atlas** | 1.1GB | `rm -rf ~/Library/Caches/com.openai.atlas/*` | OpenAI 应用缓存 |
| **Google** | 1.1GB | `rm -rf ~/Library/Caches/Google/*` | Google 应用缓存 |
| **Doubao** | 1.0GB | `rm -rf ~/Library/Caches/Doubao/*` | 豆包应用缓存 |
| **Tabbit** | 967MB | `rm -rf ~/Library/Caches/Tabbit/*` | Tabbit 应用缓存 |
| **camoufox** | 657MB | `rm -rf ~/Library/Caches/camoufox/*` | Camoufox 浏览器缓存 |

**清理命令（一次性）**：
```bash
# 清理所有应用缓存（保留目录结构）
find ~/Library/Caches -mindepth 1 -maxdepth 1 -type d -exec rm -rf {}/* \;
```

⚠️ **注意**：清理缓存后，应用下次启动可能会变慢（需要重新生成缓存）。

---

### 2. 日志文件（~/Library/Logs）

| 应用 | 日志大小 | 说明 |
|------|----------|------|
| QClaw | 9.9MB | QClaw 日志 |
| bilibili | 5.1MB | B站客户端日志 |
| LetsVPN | 15MB | LetsVPN 日志（3个文件）|
| Manus | 1.9MB | Manus 日志 |
| Qianwen | 1.1MB | 千问日志 |

**清理命令**：
```bash
# 清理所有日志文件
find ~/Library/Logs -name "*.log" -mtime +7 -delete
```

⚠️ **注意**：只删除 7 天前的日志文件，保留近期日志便于排查问题。

---

### 3. Downloads 大文件

| 文件 | 大小 | 路径 |
|------|------|------|
| .5A4RE8SF68.com.tencent.xinWeChat | 173MB | ~/Downloads/.5A4RE8SF68.com.tencent.xinWeChat |

**建议**：这是微信相关文件，请确认是否可以删除。

---

### 4. Homebrew 清理

```bash
# 清理所有 Homebrew 缓存和旧版本
brew cleanup -s --prune=all
```

**可清理内容**：
- 下载的配方缓存
- 旧版本的应用包
- 构建缓存

---

### 5. pip 缓存清理

```bash
# 清理 pip 缓存
pip cache purge
# 或者
rm -rf ~/Library/Caches/pip/*
```

---

## 🚫 不建议清理的项目

| 项目 | 原因 |
|------|------|
| ~/Library/Preferences | 应用偏好设置，删除会导致应用重置 |
| ~/Library/Application Support | 应用数据，删除会丢失应用数据 |
| ~/Documents | 个人文档，非常重要 |
| ~/Desktop | 桌面文件，非常重要 |
| ~/.Trash | 废纸篓为空，无需清理 |

---

## 📋 清理操作步骤（待你确认）

### 步骤 1：清理应用缓存（最安全）
```bash
# 清理 Playwright 缓存
rm -rf ~/Library/Caches/ms-playwright/*

# 清理 Homebrew 缓存
brew cleanup -s

# 清理 pip 缓存
pip cache purge
```

### 步骤 2：清理日志文件
```bash
# 删除 7 天前的日志
find ~/Library/Logs -name "*.log" -mtime +7 -delete
```

### 步骤 3：手动检查 Downloads
```bash
# 打开 Downloads 文件夹
open ~/Downloads
# 手动删除不需要的文件
```

### 步骤 4：验证清理效果
```bash
# 检查磁盘空间
df -h /
# 检查 Caches 目录大小
du -sh ~/Library/Caches
```

---

## ❓ 下一步操作

请告诉我：

1. **是否执行清理**？
   - ✅ 是，执行上述清理命令
   - ❌ 否，我需要修改清理方案

2. **清理范围**？
   - [ ] 仅清理应用缓存（最安全，可清理 ~10GB）
   - [ ] 清理应用缓存 + 日志文件
   - [ ] 清理全部（应用缓存 + 日志 + Downloads 旧文件）

3. **特殊要求**？
   - 需要保留某些应用的缓存？
   - 需要调整清理策略？

---

**等待你的确认后，我将执行清理操作**。🛡️